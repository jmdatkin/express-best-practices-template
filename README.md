# express-directory-structure-template

Template for a modular and maintainable Express.js app.

# To Run

1. Run `npm install`.
2. Run `npm run dev`.
3. Navigate to http://localhost:4030/users to see data.
